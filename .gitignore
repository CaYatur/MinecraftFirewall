## .NET
bin/
obj/
*.user
*.suo
.vs/

## Test results
[Tt]est[Rr]esult*/
*.trx

## Rider / VS Code
.idea/
.vscode/

## OS cruft
.DS_Store
Thumbs.db

## Local secrets / environment overrides — never commit real server data
appsettings.*.local.json
*.pfx
*.pem

## Local Stage 2 spike test server — the jar, world save, logs, and full generated reports are
## large/regeneratable local artifacts, not source. Only the small packets-*.json excerpt this
## project actually sourced IDs from is committed, under docs/protocol/.
test-server/

## Ad-hoc logs from manual live-verification runs (proxy + ProtocolSpike) — regenerated each time,
## never source.
proxy.log
spike.log

## Release build output (regenerated by dotnet publish, never committed)
publish/
*.zip

# The plugin jar is a build artefact; installer/build.ps1 produces it.
plugin/build/
